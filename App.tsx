import React, { useRef, useState } from "react";
import ReactDOM from "react-dom";

const App: React.FC = ({ blockID }) => {

    return (
        <div>
            
        </div>
    )
}
export default App